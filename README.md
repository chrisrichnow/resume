# Resume

Christopher Richnow — LaTeX source for internship/full-time resume.

## Files
- `main.tex` — resume source. Compile with any LaTeX engine (e.g. `pdflatex main.tex`) or paste into Overleaf.

## Notes
- Overleaf project: "Chris Richnow Spring 2026 FS" — this repo is not synced to it automatically (Overleaf's GitHub Sync/Git integration require a paid plan). To update Overleaf, copy `main.tex` contents into the Overleaf editor manually and recompile.
- Contact links in the header (email, LinkedIn, GitHub) use `\href{}` — if updating them, double check both the display text and the actual link target match.
